name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  api:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: recoveryos
          POSTGRES_PASSWORD: recoveryos_dev
          POSTGRES_DB: recoveryos
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready -U recoveryos -d recoveryos
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://recoveryos:recoveryos_dev@localhost:5432/recoveryos?schema=public
      PORT: 4777
      HOST: 127.0.0.1
      LOG_LEVEL: silent

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint -w @recoveryos/api

      - name: Typecheck
        run: npm run typecheck -w @recoveryos/api

      - name: Test
        run: npm run test -w @recoveryos/api

      - name: Build
        run: npm run build -w @recoveryos/api

  web:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint -w @recoveryos/web

      - name: Typecheck
        run: npm run typecheck -w @recoveryos/web

      - name: Test
        run: npm run test -w @recoveryos/web

      - name: Build
        run: npm run build -w @recoveryos/web
